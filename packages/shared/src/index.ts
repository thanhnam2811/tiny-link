import { APP_VERSION } from './version';

export const sharedConfig = {
	appName: 'TinyLink',
	version: APP_VERSION,
};

export * from './version';

export * from './constants';
export * from './schemas';
